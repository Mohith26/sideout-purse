# Workspace packages are consumed as TypeScript source; keep peer resolution strict.
auto-install-peers=true
strict-peer-dependencies=false
# Every package in the workspace pins the same Node; fail fast on the wrong one.
engine-strict=true
