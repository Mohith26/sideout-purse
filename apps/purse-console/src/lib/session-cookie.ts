/** The console's session cookie name and the header the middleware carries the current path on; Web-API-only so the edge middleware can import it. */
export const SESSION_COOKIE = 'purse_console_session';
export const CONSOLE_PATH_HEADER = 'X-Console-Path';
