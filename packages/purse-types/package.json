{
  "name": "@purse/types",
  "version": "0.0.1",
  "private": true,
  "description": "Public Purse types: the API error taxonomy, header names, the sealed eligibility vocabulary, the v1 resource shapes, the iframe message protocol, the webhook event shapes and the signed score attestation contract (canonical form, key id, sign and verify). The only Purse package besides @purse/sdk that a partner app may import.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "fast-check": "^4.10.1",
    "vitest": "^5.0.1"
  },
  "dependencies": {
    "zod": "^4.6.5"
  }
}
